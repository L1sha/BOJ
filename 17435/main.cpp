#include "bits/stdc++.h"


int M, Q, N, X;
std::array<int, 200'001> F;




int main(int argc, char** argv) {
    
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(nullptr);
    std::cout.tie(nullptr);
    
    std::cin >> M;
    
    for (int idx = 1; idx <= M; ++idx) {
        
        std::cin >> F[idx];
    }
    
    std::cin >> Q;
    
    while (Q--) {
        
        std::cin >> N >> X;
        
        
    }
    
    return 0;
}

